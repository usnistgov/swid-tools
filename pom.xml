<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<groupId>gov.nist.decima</groupId>
	<artifactId>swidval</artifactId>
	<version>0.0.1-SNAPSHOT</version>

	<scm>
		<connection>scm:svn:https://subversion.nist.gov/external1/NVD/swidval/trunk</connection>
		<developerConnection>scm:svn:https://subversion.nist.gov/external1/NVD/swidval/trunk</developerConnection>
		<url>https://subversion.nist.gov/external1/NVD/swidval/trunk</url>
	</scm>

	<properties>
		<decima.version>0.0.1-SNAPSHOT</decima.version>
	</properties>

	<dependencies>
		<dependency>
			<groupId>gov.nist.decima</groupId>
			<artifactId>core</artifactId>
			<version>${decima.version}</version>
		</dependency>
		<dependency>
			<groupId>gov.nist.decima</groupId>
			<artifactId>testing</artifactId>
			<version>${decima.version}</version>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>gov.nist.decima</groupId>
			<artifactId>module</artifactId>
			<version>${decima.version}</version>
		</dependency>
		<dependency>
			<groupId>junit</groupId>
			<artifactId>junit</artifactId>
			<version>4.12</version>
			<scope>test</scope>
		</dependency>
	</dependencies>

	<build>
		<resources>
			<resource>
				<directory>src/main/resources</directory>
			</resource>
			<resource>
				<directory>src/main/xsd</directory>
				<includes>
					<include>**/*.xsd</include>
				</includes>
			</resource>
		</resources>

		<pluginManagement>
			<plugins>
				<!--This plugin's configuration is used to store Eclipse m2e settings 
					only. It has no influence on the Maven build itself. -->
				<plugin>
					<groupId>org.eclipse.m2e</groupId>
					<artifactId>lifecycle-mapping</artifactId>
					<version>1.0.0</version>
					<configuration>
						<lifecycleMappingMetadata>
							<pluginExecutions>
								<pluginExecution>
									<pluginExecutionFilter>
										<groupId>org.apache.maven.plugins</groupId>
										<artifactId>maven-dependency-plugin</artifactId>
										<versionRange>[0.0,)</versionRange>
										<goals>
											<goal>unpack</goal>
										</goals>
									</pluginExecutionFilter>
									<action>
										<ignore />
									</action>
								</pluginExecution>
							</pluginExecutions>
						</lifecycleMappingMetadata>
					</configuration>
				</plugin>
			</plugins>
		</pluginManagement>

		<plugins>
			<plugin>
				<!-- Used to pull XSD files from the JAR -->
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-dependency-plugin</artifactId>
				<executions>
					<execution>
						<id>unpack-xsd-files</id>
						<!-- Using the initialize phase because it is before the generate sources 
							phase -->
						<phase>initialize</phase>
						<goals>
							<goal>unpack</goal>
						</goals>
						<configuration>
							<artifactItems>
								<artifactItem>
									<!-- Artifact that has the schema files -->
									<groupId>gov.nist.decima</groupId>
									<artifactId>core</artifactId>
									<version>${decima.version}</version>
									<type>jar</type>
								</artifactItem>
							</artifactItems>
							<includes>**/*.xsd</includes>
							<outputDirectory>${project.basedir}/target/xsd-includes</outputDirectory>
						</configuration>
					</execution>
					<execution>
						<id>unpack-bootstrap</id>
						<phase>initialize</phase>
						<goals>
							<goal>unpack</goal>
						</goals>
						<configuration>
							<artifactItems>
								<artifactItem>
									<groupId>gov.nist.decima</groupId>
									<artifactId>core</artifactId>
									<version>${decima.version}</version>
									<classifier>bootstrap</classifier>
									<type>jar</type>
								</artifactItem>
							</artifactItems>
							<outputDirectory>${project.build.directory}/bootstrap</outputDirectory>
						</configuration>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
				<version>3.3</version>
				<configuration>
					<source>1.8</source>
					<target>1.8</target>
				</configuration>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-surefire-plugin</artifactId>
				<version>2.18.1</version>
				<configuration>
					<includes>
						<include>**/AssessmentTestSuite.class</include>
					</includes>
				</configuration>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-jar-plugin</artifactId>
				<!--  TODO: Update to 3.0.2 once the m2e bug is fixed -->
				<version>3.0.0</version>
				<configuration>
					<archive>
						<manifest>
							<mainClass>gov.nist.decima.swid.Application</mainClass>
							<addClasspath>true</addClasspath>
							<classpathPrefix>lib/</classpathPrefix>
							<classpathLayoutType>custom</classpathLayoutType>
							<customClasspathLayout>${artifact.artifactId}-${artifact.baseVersion}${dashClassifier?}.${artifact.extension}</customClasspathLayout>
						</manifest>
					</archive>
				</configuration>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-assembly-plugin</artifactId>
				<version>2.6</version>
				<!-- <configuration> -->
				<!-- <archive> -->
				<!-- <manifest> -->
				<!-- <mainClass>gov.nist.decima.swid.Application</mainClass> -->
				<!-- </manifest> -->
				<!-- </archive> -->
				<!-- </configuration> -->
				<dependencies>
					<dependency>
						<groupId>gov.nist</groupId>
						<artifactId>assembly-swid-generator</artifactId>
						<version>0.0.2-SNAPSHOT</version>
					</dependency>
				</dependencies>
				<executions>
					<!-- <execution> -->
					<!-- <id>make-assembly</id> this is used for inheritance merges -->
					<!-- <phase>package</phase> bind to the packaging phase -->
					<!-- <goals> -->
					<!-- <goal>single</goal> -->
					<!-- </goals> -->
					<!-- <configuration> -->
					<!-- <descriptorRefs> -->
					<!-- <descriptorRef>jar-with-dependencies</descriptorRef> -->
					<!-- </descriptorRefs> -->
					<!-- </configuration> -->
					<!-- </execution> -->
					<execution>
						<id>make-assembly-bin</id> <!-- this is used for inheritance merges -->
						<phase>package</phase> <!-- bind to the packaging phase -->
						<goals>
							<goal>single</goal>
						</goals>
						<configuration>
							<descriptors>
								<descriptor>src/assembly/bin.xml</descriptor>
							</descriptors>
						</configuration>
					</execution>
				</executions>
			</plugin>
		</plugins>
	</build>
</project>